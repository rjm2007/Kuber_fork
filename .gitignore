.next
node_modules
.env*.local
# Real secrets prepared for Vercel import — never commit
.env.vercel
out
dist
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
ui-screenshots
# Ad-hoc Playwright verification artifacts — never product code
verify-shots
verify-ui.mjs
*.tsbuildinfo
# Local QA handoff notes — contains team PII, must never hit this public repo
TESTING_HANDOFF.md
.vercel

# Playwright run artifacts
scripts/e2e-shots/
